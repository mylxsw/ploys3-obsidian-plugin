{
  "name": "ploys3-uploader",
  "version": "0.1.0",
  "description": "Obsidian upload plugin for PloyS3 App, focused on uploading note images to S3-compatible storage and rewriting links.",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp dist/main.js main.js",
    "dev": "tsc -p tsconfig.json -w"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "obsidian": "^1.7.0",
    "typescript": "^5.7.3"
  }
}
